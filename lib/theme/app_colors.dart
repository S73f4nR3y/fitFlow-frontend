import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary colors
  static const primary = Color(0xFF6B4EFF);
  static const primaryLight = Color(0xFF9D8AFF);
  static const primaryDark = Color(0xFF4A35B3);

  // Secondary colors
  static const secondary = Color(0xFFFF6B6B);
  static const secondaryLight = Color(0xFFFF9999);
  static const secondaryDark = Color(0xFFCC5555);

  // Neutral colors
  static const background = Color(0xFFF5F5F5);
  static const surface = Color(0xFFFFFFFF);
  static const surfaceVariant = Color(0xFFF0F0F0);
  static const card = Color(0xFFFFFFFF);

  // Text colors
  static const textPrimary = Color(0xFF1A1A1A);
  static const textSecondary = Color(0xFF666666);
  static const textTertiary = Color(0xFF999999);
  static const textOnPrimary = Color(0xFFFFFFFF);
  static const textOnSecondary = Color(0xFFFFFFFF);

  // Status colors
  static const success = Color(0xFF4CAF50);
  static const error = Color(0xFFE53935);
  static const warning = Color(0xFFFF9800);
  static const info = Color(0xFF2196F3);

  // Border colors
  static const border = Color(0xFFE0E0E0);
  static const borderLight = Color(0xFFF0F0F0);

  // Gradient
  static const gradientStart = Color(0xFF6B4EFF);
  static const gradientEnd = Color(0xFFFF6B6B);

  // Shimmer
  static const shimmerBase = Color(0xFFE0E0E0);
  static const shimmerHighlight = Color(0xFFF5F5F5);
}
