import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary colors - Futuristic cyan/neon
  static const primary = Color(0xFF00F5FF);
  static const primaryLight = Color(0xFF66F9FF);
  static const primaryDark = Color(0xFF00C4CC);

  // Secondary colors
  static const secondary = Color(0xFFBF00FF);
  static const secondaryLight = Color(0xFFD966FF);
  static const secondaryDark = Color(0xFF8F00CC);

  // Neutral colors - Dark theme
  static const background = Color(0xFF0A0A0F);
  static const surface = Color(0xFF121218);
  static const surfaceVariant = Color(0xFF1E1E2E);
  static const card = Color(0xFF16161F);

  // Text colors
  static const textPrimary = Color(0xFFFFFFFF);
  static const textSecondary = Color(0xFFB0B0B0);
  static const textTertiary = Color(0xFF707080);
  static const textOnPrimary = Color(0xFF0A0A0F);
  static const textOnSecondary = Color(0xFF0A0A0F);

  // Status colors
  static const success = Color(0xFF00FF94);
  static const error = Color(0xFFFF3366);
  static const warning = Color(0xFFFFD600);
  static const info = Color(0xFF00B4FF);

  // Border colors
  static const border = Color(0xFF2A2A3E);
  static const borderLight = Color(0xFF1E1E2E);

  // Gradient
  static const gradientStart = Color(0xFF00F5FF);
  static const gradientEnd = Color(0xFFBF00FF);

  // Shimmer
  static const shimmerBase = Color(0xFF2A2A3E);
  static const shimmerHighlight = Color(0xFF1E1E2E);
}
